import styled from 'styled-components';
import tw from 'twin.macro';

export const StyledHomeHeaderWrapper = styled.header`
  ${tw`flex justify-between items-center px-4 mx-auto relative z-50 bg-black`}
`;

export const LogoWrapper = styled.div`
  ${tw``}
`;

export const MenuWrapper = styled.div`
  ${tw`mr-auto mx-1`}
`;

export const CatagoryWrpper = styled.div`
  ${tw`mx-4`}
`;

export const ProfileAndNotificationWrapper = styled.div`
  ${tw`flex justify-between`}
`;

// lg:(flex justify-between items-center)
